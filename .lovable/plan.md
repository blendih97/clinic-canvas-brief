# Reposition translator pages around the RinVita medical hub

## Changes
- Add the supplied medical-hub positioning line beneath the introduction on `/translate` and every language-specific translator page.
- Replace the below-tool call-to-action on all translator pages with the supplied hub-focused heading, body, button, link, and unchanged free-plan support line.
- Update the successful-translation card with the supplied heading and body while retaining the existing signup destination.
- Add a four-item “What your RinVita hub does” strip after the glossary on language pages and after the tool on `/translate`; all four listed capabilities already exist in RinVita.
- Rewrite the opening of `llms.txt` so RinVita leads as a private medical-records hub and translation is one capability.
- Add focused copy assertions, run the production build, and inspect generated translator HTML for the new text.

## Boundaries
Pricing, the signed-in app, authentication, payments, admin, glossaries, and prerender implementation remain unchanged.
